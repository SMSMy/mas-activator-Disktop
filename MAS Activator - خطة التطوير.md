<div dir="rtl">

# خطة تطوير مشروع MAS Activator — النسخة المدمجة النهائية

> مدمجة من الخطة الأصلية + وثيقة المراجعة، مع تطبيق التحفظات: بند 2.2 أصبح ملموسًا، التحديث الموقّع مؤجل كهدف نهائي مع بديل عملي مؤقت، قاعدة اختيار منتج Windows محددة، ومرحلة الصفر بقائمة تحقق خفيفة.

**المبدأ الحاكم:** تثبيت صحة النتيجة وأمان التنفيذ وقابلية الاختبار قبل أي توسع في الميزات — لا نجاح كاذب، ولا عمليات معلّقة، ولا تنزيلات غير قابلة للتدقيق، ولا حالات واجهة متناقضة.

---

## المرحلة 0: خط الأساس ومعايير القبول

قبل أي تعديل، وثّق سلوك النسخة الحالية (خاصة ما نُفّذ فعلًا مثل `build.rs`)، ثم اعتمد قائمة التحقق التالية. شكلها: **قائمة يدوية + أتمتة الرخيص فقط** (`pnpm check` و`cargo clippy`) — لا أتمتة كاملة للمصفوفة في هذه المرحلة.

**حد أدنى من CI من اليوم الأول**: أنشئ workflow أوليًا على Windows (GitHub Actions) يشغّل `pnpm check` + بناء الواجهة + `cargo test` + `cargo clippy`، ليحمي كل تعديل في المراحل 1–5 من الانحدار، ويُوسَّع في المرحلة 6 بفحص الأسرار وملفات البناء وبناء Release.

| مجال الاختبار | سيناريوهات القبول |
|---|---|
| حالة الترخيص | Windows بالعربية والإنجليزية (VM يدوي)، منتجات متعددة، مرخص / غير مرخص / فترة سماح / تعذر الاستعلام |
| الصلاحيات | تشغيل عادي (يتطلب الـ manifest فتح UAC)، رفض رفع الصلاحية، تشغيل بصلاحية مسؤول |
| الشبكة | متاحة، منقطعة، مهلة، استجابة غير صالحة |
| إدارة العمليات | إلغاء أثناء التشغيل، انتهاء مهلة، إغلاق النافذة، ثم تشغيل فحص جديد مباشرة |
| الواجهة | عدم تداخل مؤشرات التحميل، نتيجة مفهومة لكل حالة خطأ |
| البناء | `pnpm check`، بناء الواجهة، `cargo test`، `cargo clippy` |

---

## المرحلة 1: الإصلاحات الحرجة — صحة النتيجة أولًا

### 1.1 إصلاح ترميز `tauri.conf.json`

- **التحقق أولًا**: فحص الحالة الحالية للملف (المستودع قد يختلف عن نسخة المراجعة).
- **الحل**: إعادة حفظ الملف بـ UTF-8 (بدون BOM) والعنوان الصحيح: `"MAS Activator - أداة تفعيل شاملة"`.
- **الملفات**: `src-tauri/tauri.conf.json`.
- **القبول**: عنوان النافذة سليم في exe الناتج.

### 1.2 جعل «فحص الحالة» نواة المنتج — نموذج بيانات منظم

- **المشكلة**: `lib.rs:263-286` يعتمد على نصوص `slmgr /xpr` الإنجليزية + يرجع سلسلة نصية.
- **الحل — هيكل Rust واضح بدل نص**:

```rust
struct StatusReport {
    selected: ProductStatus,        // المنتج المختار (الملخص في الواجهة)
    observed: Vec<ProductStatus>,   // المنتجات المرصودة (التفاصيل في نافذة الحالة/التقرير)
    checked_at: String,             // وقت الفحص
    error: Option<StatusError>,     // None / Partial / QueryFailed (مصنفة)
}

struct ProductStatus {
    kind: ProductKind,              // Windows / Office
    name: String,                   // اسم المنتج
    state: LicenseState,            // Activated / Notification / Grace(days) / NotLicensed / Unknown
    grace_days: Option<u32>,        // المتبقي من فترة السماح
    selection_reason: String,       // لماذا اختير أساسيًا (وفق قاعدة الاختيار)
}
```

- **استبدال slmgr بـ CIM** بنفس جدول أولويات `LicenseStatus` المستخدم لأوفيس.
- **قاعدة اختيار منتج Windows الصريحة** (قابلة للاختبار):
  1. تصفية `ApplicationId = '55c92734-d682-4d71-983e-d6ec3f16059f'` (Windows فقط).
  2. شرط `PartialProductKey IS NOT NULL` أولًا، ثم أي سجل بنشاط إن لم يوجد.
  3. استبعاد إدخالات `Windows Insider` والمنتجات الفرعية.
  4. ترتيب بنفس جدول الأولوية (`1 > 2,6 > 3 > 4 > 5 > 0`).
  5. دعم `GracePeriodRemaining` لعرض الأيام المتبقية.
- **الملفات**: `src-tauri/src/lib.rs`، `client/src/pages/Home.tsx` + `StatusDialog.tsx`.
- **القبول**: نتيجة مطابقة لـ `slmgr /dli` على نظام عربي وإنجليزي، ومنتجات متعددة.

### 1.3 حالات عملية دقيقة بدل «نجح/فشل» + تحقق لاحق

- **المشكلة**: `$LASTEXITCODE` غير موثوق بعد استدعاء ScriptBlock، و«تم التنفيذ» قد تُقرأ كنجاح فعلي.
- **الحل**: نتيجة العملية واحدة من حالات مصنفة:

```
تم التحقق من التغيير ✅ | لم يتغير الوضع ⚠️ | تعذر التحقق ❓ |
أُلغي | انتهت المهلة | يتطلب صلاحية مسؤول | لا يوجد اتصال
```

- **التحقق اللاحق**: بعد انتهاء العملية، فحص CIM قصير ومحدود (يُعاد المحاولة حتى مرتين بفاصل 3 ثوانٍ لأن الحالة قد لا تتحدث لحظيًا)، وتعرض الواجهة **وقت الفحص** بجانب النتيجة. هذا يفصل «فشل العملية» عن «فشل التحقق منها».
- **تحسين التنفيذ**: تشغيل MAS في عملية PowerShell فرعية لعزل `exit` وضمان التقاط كود الخروج.
- **الملفات**: `src-tauri/src/lib.rs` (كل دوال `activate_*` + `execute_activation`)، `Home.tsx`.
- **القبول**: فشل التفعيل لا يظهر نجاحًا أبدًا؛ والنتيجة «لم يتغير الوضع» تظهر بوضوح عند عدم تغير الترخيص.

### 1.4 اختبارات وحدات للمنطق القابل للعزل

`cargo test` بلا اختبارات لا قيمة له — أضف اختبارات صغيرة محددة في المرحلة 1. لا تُؤتمت التفعيل نفسه داخل CI؛ سيناريوهات Windows الحقيقية تبقى في القائمة اليدوية/VM:

| الوحدة | الاختبار المطلوب |
|---|---|
| تحويل `LicenseStatus` | كل قيمة معروفة تتحول إلى التسمية والأولوية المتوقعة |
| اختيار منتج Windows | تعدد منتجات، منتج Insider، سجل بلا مفتاح جزئي، فترة سماح |
| نتائج العملية | الفشل، الإلغاء، انتهاء المهلة، تعذر التحقق، عدم تغير الحالة |
| التحقق من المسار | اسم عادي، محاولة خروج من المجلد، مسار بعد تسوية لا يقع داخل مجلد التنزيلات |

---

## المرحلة 2: الصلاحيات وتجربة التشغيل

### 2.1 تدقيق UAC الحالي وتحسينه (وليس البناء من الصفر)

- **الواقع**: `src-tauri/build.rs` يضمّن فعلًا manifest بـ `requireAdministrator` — التطبيق لا يبدأ أصلًا بدون موافقة UAC.
- **المطلوب**:
  1. تدقيق أن الـ manifest يُضمَّن في الـ release build (portable + NSIS).
  2. أمر `check_admin` وقت التشغيل (فحص `IsUserAnAdmin`) كحزام أمان: إن حدث تشغيل بلا صلاحية (بيئات غير قياسية) يعرض بانر واضح بدل أخطاء PowerShell خام.
  3. **سيناريو رفض UAC**: مع `requireAdministrator` يمنع الرفض بدء العملية أصلًا — لا يمكن للتطبيق عرض أي رسالة بعد الرفض. القرار المعتمد: توثيق أن الرفض = إنهاء الإطلاق بلا رسالة داخلية، واعتماد «مشغّل صغير غير مرتفع الصلاحية» (يعرض التوجيه ثم يعيد الإطلاق كمسؤول) كتطوير مستقبلي. لا تُجعل رسالة الواجهة شرط قبول لحالة لا تصل فيها الواجهة إلى مرحلة التشغيل.
- **الملفات**: `src-tauri/build.rs` (تدقيق)، `src-tauri/src/lib.rs`، `Home.tsx`.
- **القبول**: سيناريوهات الصلاحيات الثلاثة في مصفوفة المرحلة 0 تعمل كما هو متوقع.

### 2.2 آلة حالات موحدة للواجهة

- **المشكلة**: `Home.tsx:215` — حالات تحميل منفصلة قد تتعارض.
- **الحل**: حالة واحدة للعملية الحالية: `idle | checkingStatus | running | cancelling | succeeded | failed | cancelled`.
- **قواعد**:
  - منع بدء أي عملية متعارضة أثناء وجود عملية نشطة.
  - الفحص مستقل فقط إن كان آمنًا ومقصودًا؛ وإلا يُعرض بوضوح أنه فحص «قبل/بعد» العملية وليس متزامنًا غير مضمون.
- **الملفات**: `client/src/pages/Home.tsx`.
- **القبول**: لا تداخل مؤشرات تحميل؛ كل حالة خطأ لها نتيجة مفهومة.

---

## المرحلة 3: إدارة العمليات

### 3.1 المهلات والإلغاء والتنظيف

- **المشكلة**: `irm` قد يعلق للأبد، لا زر إلغاء، وإغلاق التطبيق يترك PowerShell يتيمًا.
- **الحل**:
  1. مهلات افتراضية (60 ثانية فحص / 10 دقائق تفعيل) عبر `tokio::time::timeout` — مع تنبيه صريح: **انتهاء المهلة لا يعني إنهاء العملية**؛ `timeout` يلغي الانتظار فقط بينما يبقى PowerShell يعمل. عند انتهاء المهلة: تُرسل إشارة إلغاء إلى المعرّف المحتفَظ به، ثم تُنهى شجرة العملية، ويُنتظر تأكيد الإنهاء قبل تحرير الحالة وإعلان `timeout`. لا يُعامل مستقبل المهلة كقاتل عملية.
  2. أمر `cancel_operation`: حفظ معرّف العملية ونوعها ووقت بدئها في `AppState`، وقتل **شجرة العملية** لا الأب فقط (`taskkill /T`).
  3. تحرير الحالة دائمًا — عند الخطأ وعند إغلاق النافذة (`on_window_event`) — ومنع عملية جديدة من استخدام حالة قديمة بعد الإلغاء.
  4. الإلغاء **حالة رسمية** في الواجهة (زر + حالة `cancelled`)، لا قتل صامت ثم رسالة فشل عامة.
- **الملفات**: `src-tauri/src/lib.rs`، `Home.tsx`، مكوّن زر إلغاء.
- **القبول**: سيناريوهات الإلغاء/المهلة/الإغلاق في مصفوفة المرحلة 0.

---

## المرحلة 4: استراتيجية المحتوى والتنزيلات

### 4.1 تحميل سكربتات MAS — سياسة ملموسة قابلة للتدقيق

> **الحالة: ✅ منفذ.** التنفيذ: التحميل من الرابط الرسمي في README المصدر (Azure DevOps)، التحقق من SHA-256 المثبت في الكود (`850F9796...`)، كاش في `%LOCALAPPDATA%\MAS Activator\cache\MAS_AIO.cmd`، والتشغيل من الملف المحلي عبر `cmd /D /S /C` — بلا تحميل حي ولا ScriptBlock. عدم التطابق = منع التشغيل؛ فشل التنزيل مع كاش سليم = تشغيل أوفلاين. ميزة جانبية: التخلص من حجب AMSI الذي كان يصيب `[ScriptBlock]::Create`.

- **المشكلة**: تنزيل كود وتنفيذه من الإنترنت كل تشغيل = مخاطرة توريد + عطل أوفلاين لطرق لا تحتاج اتصالًا (Ohook/TSforge).
- **الحل — خطوات ملموسة** (لا تغيير ترخيص الآن؛ قرار GPL مؤجل لما بعد تدقيق رسمي للرخص):
  1. **مصدر محدد**: التحميل من release مثبّت في مستودع MAS الرسمي (`massgravel/Microsoft-Activation-Scripts`)، لا من رابط متغير.
  2. **تحقق من سلامة الملف وفق الإصدار المحدد**: مقارنة SHA-256 المحسوب مع القيمة المنشورة في ملاحظات الإصدار، مع توثيق أن هذا يكشف تلف التنزيل أو اختلاف الملف، **ولا يحل محل توقيع رقمي مستقل أو سلسلة ثقة خارجية** (إن أُخذ الملف والهاش من القناة نفسها فليسا مستقلين فعليًا). MAS لا ينشر تواقيع رقمية أصلًا، فسقف التحقق الواقعي هو hash-pinning.
  3. **تخزين محلي مؤقّت** مع حفظ نسخة معلوماتية: الإصدار المستخدم + hash + وقت التنزيل، تُعرض في السجل.
  4. **فصل «التنزيل» عن «التشغيل»** في البنية: كل مرحلة قابلة للتسجيل والإلغاء ومعالجة فشلها بوضوح.
  5. **سلوك fallback محدد**: فشل التنزيل أو عدم تطابق hash = **منع التشغيل** برسالة واضحة (لا تنفيذ كود غير مُتحقق منه).
- **الملفات**: `src-tauri/src/lib.rs` (+ مجلد موارد/كاش).
- **القبول**: فصل الإنترنت → تفعيل Ohook يعمل من النسخة المخزنة؛ تعديل النسخة المحلية → منع التشغيل؛ بيانات الإصدار والهاش ووقت التنزيل تظهر في السجل المنظم.

### 4.2 مسار التنزيلات الموثوق

- **الحل**: جلب مجلد التنزيلات الفعلي بطريقة موثوقة (فحص `HKCU\...\Shell Folders` أو crate `dirs`)، ثم بعد تسوية المسار التحقق أن الوجهة النهائية **ما زالت داخل** ذلك المجلد (وليس مجرد رفض `..`).
- **الملفات**: `src-tauri/src/lib.rs` (دالة `download_update`).

### 4.3 فحص التحديثات — تمييز الحالات

- **الحل**: فصل «لا يوجد تحديث» عن «تعذر التحقق»؛ رسائل مختلفة لحالات: rate limit (403/429)، انقطاع الشبكة، استجابة غير متوقعة — مع تسجيل تشخيصي لا يكشف بيانات حساسة.
- **الملفات**: `src-tauri/src/lib.rs` (دالة `check_update`)، `useUpdateCheck.ts`.

---

## المرحلة 5: التنظيف والسجلات

### 5.1 حذف المثبت عدم استخدامه فقط

- **الحذف المؤكد**: `server/index.ts` + express وaxios (غير مستخدمة في تطبيق Tauri).
- **الملفات**: `server/`, `package.json`, `pnpm-lock.yaml`.

### 5.2 سجل الخلفية: إما حذف فعلي أو تطوير — لا حالة وسطى

- **القرار**: إن ثبت أن `AppState.logs` + `get_logs` + `clear_logs` بلا أي مستخدم → **حذف فعلي**. وإلا تطويره إلى **سجل منظّم ومحدود الحجم** يربط كل سطر بمعرّف العملية ونوعها ووقتها وحالتها، ويُستخدم لاحقًا في التقرير التشخيصي بعد التنقيح.
- **الملفات**: `src-tauri/src/lib.rs`.

### 5.3 تنظيف بقايا أدوات التطوير

- **الحذف من المستودع**: `client/public/__manus__/debug-collector.js`، `vite-plugin-manus-runtime` من dependencies، `.manus-logs/`، `.webdev/`، `dist/`، `node_modules/`، `MAS.Activator_2.1.0.Portable.exe`.
- **إضافة لـ `.gitignore`**: `node_modules/` (تغطية صحيحة) و `client/public/__manus__/`.
- **الملفات**: `.gitignore`, `package.json`.

### 5.4 توحيد مصدر الإصدار

- **الحل**: `getVersion()` من `@tauri-apps/api/app` بدل "الإصدار 2.1" المثبت يدويًا في `Home.tsx:171`.

### 5.5 إزالة dependencies غير المستخدمة

- **المرشحون للحذف**: `recharts`, `react-hook-form`, `zod`, `@hookform/resolvers`, `react-day-picker`, `input-otp`, `vaul`, `cmdk`, `streamdown`, `react-resizable-panels`, `@types/google.maps`.
- **القبول**: `pnpm check` + `pnpm build` ناجحان + مقارنة حجم bundle قبل/بعد.

---

## المرحلة 6: CI وبوابة الجودة

> **الحالة: ✅ منفذ.** الـ workflow يشمل الآن: فحص TypeScript + بناء الواجهة + `cargo test --lib` + `clippy --lib -D warnings` (على كل push/PR)، ووظيفة **guard** تمنع إدخال ملفات بناء/تنفيذ وأنماط أسرار ظاهرة، ووظيفة **Release** تُبنى وتُرفع مثبّت NSIS عند الوسوم `v*`.

توسعة الـ workflow الأولي الذي أُنشئ في المرحلة 0 — يضاف إليه:

1. فحص يمنع إدخال ملفات بناء وتنفيذ ومجلدات تبعيات وأسرار إلى المستودع (git-secrets / قواعد ignore)
2. بناء Release وفحصه قبل النشر (عند تفعيل مسار التحديث في 7.1)

---

## المرحلة 7: الميزات (فوق أساس مستقر)

### 7.1 التحديث التلقائي

> **قرار التوزيع ✅**: الأساسي = **Portable** (`MAS.Activator_x.y.z.Portable.exe` — الاستخدام المتوقع لمرة واحدة) يُنتج في الـ CI بإعادة تسمية الـ exe الخام، مع حساب SHA-256 ونشره. الثانوي = مثبّت NSIS (مهم لأنظمة تفتقد WebView2 Runtime). فاحص التحديث يفضّل أصل Portable. التوقيع الرقمي ما زال مؤجلًا بقرار الميزانية.

- **الهدف النهائي**: Installer موقّع + تحديثات موقعة (يتطلب شهادة Code Signing مدفوعة — قرار ميزانية لاحق).
- **المسار المؤقت الآن**: الإبقاء على Portable + نشر SHA-256 لكل إصدار في ملاحظات الإصدار، مع التحقق منه قبل تشغيل التحديث المحمّل — مع نفس التحفظ: هذا تحقق سلامة وليس بديلًا عن توقيع مستقل.
- **الملفات**: حسب القرار (`tauri-plugin-updater` عند اختيار الـ Installer).

### 7.2 فحص تلقائي بعد التفعيل

- بعد أي عملية، عرض نتيجة التحقق اللاحق (البند 1.3) تلقائيًا في نافذة «حالة التفعيل» مع وقت الفحص، وعرض **الفرق بين حالة ما قبل العملية وما بعدها** — لا الحالة النهائية وحدها.

### 7.3 نافذة تأكيد قبل العمليات

- Dialog بعنوان العملية ووصفها قبل التنفيذ. مكوّن جديد `ConfirmDialog.tsx`.

### 7.4 تقرير تشخيصي آمن

- زر «حفظ التقرير»: اختياري + **معاينة قبل الحفظ** + استبعاد البيانات الحساسة (اسم المستخدم، المسارات الكاملة، المفاتيح الجزئية، الرموز، الرؤوس). يخدم الدعم الفني فقط.
- **الملفات**: أمر `export_logs` + `Home.tsx`.

### 7.5 (اختياري) صفحة إعدادات

- حفظ الخيارات (الثيم، فحص التحديثات عند الإطلاق، سلوك الكاش) عبر `localStorage` أو `tauri-plugin-store`.

### 7.6 تغيير إصدار ويندوز (Change Edition) — دفعتان

#### 7.6-A — الاستكشاف والتحقق (منفذة)

- **الهدف**: استكشاف الإصدار الحالي والوجهات المدعومة، مع **فصل صريح** بين تغيير الإصدار وحالة الترخيص (ليست «ترقية وترخيص مضمون»).
- **ما نُفذ**: قراءة `ProductName`/`EditionID`/`DisplayVersion`/البناء من السجل + `dism /online /english /Get-TargetEditions` + حالة الترخيص من النموذج المنظم + حواجز المسارات (Non-Core→Core، CountrySpecific، ServerRdsh، CloudEdition) + زر «فتح إعدادات تنشيط Windows» (المسار الرسمي لإدخال مفتاح المستخدم بنفسه) + تحقق صادق قبل/بعد + حالة `pending_restart` (إشارة مساعدة من سجلي `PendingFileRenameOperations` و`RebootPending`) + تنقيح أي نمط مفتاح من السجلات.
- **لا يوجد**: حقل مفتاح في التطبيق، ولا مفاتيح في أوامر أو سجلات.

#### 7.6-B — تنفيذ مباشر عبر ثنائيات Windows الرسمية (بلا MAS وبلا قوائم)

> **لماذا؟** تحقق من مصدر MAS: لا يملك أي مفتاح unattended لتغيير الإصدار (الـ AIO يوجّه `/HWID` و`/Z-` و`/K-` و`/Ohook` فقط، وقائمة التغيير تفاعلية حصرًا `set /p`)، والتطبيق واجهة بديلة عن استخدام MAS عبر cmd.
>
> يختار المستخدم الإصدار الهدف من قائمة الوجهات المدعومة داخل واجهة التطبيق، ثم يُنفَّذ التحويل بصمت عبر أدوات Windows الرسمية، ويتحقق التطبيق من النتيجة فعليًا.

- **الآلية** (سلوك Windows موثق، لا كود منقول من MAS):
  1. استرجاع مفتاح الإصدار **العام** من `pkeyhelper.dll` وقت التشغيل (`GetEditionIdFromName` + `SkuGetProductKeyForEdition`) — لا مفاتيح مضمّنة في الكود.
  2. اختيار المسار (منطق نقي قابل للاختبار): صورة خادم → `DISM /Set-Edition`؛ Core→غير Core → `changepk.exe /ProductKey`؛ البقية وSKU tokens موجودة → `slmgr /ipk`؛ بناء <17134 أو تقييم → مرفوض.
  3. المفتاح يمر عبر **متغير بيئة** للعملية الفرعية — لا يظهر في سطر الأوامر، ولا يُسجَّل (تنقيح نمط المفتاح مطبق على كل المخرجات).
  4. تنفيذ خفي (`CREATE_NO_WINDOW`) ضمن إدارة العمليات: إلغاء يقتل الشجرة، مهلة 30 دقيقة، وفحص نهائي منظم بعد انتهاء العملية الفعلية.
  5. النتيجة الصادقة: `pending_restart` له أولوية على أي نجاح، والتحقق بعد إعادة التشغيل هو الحكم النهائي.
- **الواجهة**: منتقي إصدارات بأزرار قابلة للاختيار + **تلميحات (tooltips)** لكل زر + لوحة تأكيد + عدّاد أثناء التنفيذ + زر إلغاء.
- **المسار الرسمي البديل**: «فتح إعدادات تنشيط Windows» يبقى لمن يريد إدخال مفتاحه الشخصي بنفسه.
- **حالات النتيجة**: `edition_changed_and_activated` / `edition_changed_needs_activation` / `pending_restart` / `edition_unchanged` / `unsupported_path` / `discovery_failed` / `verification_failed` / `cancelled` / `timed_out`.
- **الملفات**: `lib.rs` (أمر `change_edition` + `select_change_method` + إعادة استخدام `cancel_operation`)، `EditionDialog.tsx`.
- **القبول**: على VM بلقطة استعادة: Core→Pro عبر changepk، Pro→Workstation عبر slmgr، إلغاء، مهلة — مع التحقق أن أي سجل أو تقرير لا يحتوي مفتاحًا (عامًا كان أو شخصيًا).

---

## القرارات النهائية

| القرار | التوصية النهائية |
|---|---|
| دمج سكربتات MAS محليًا (ترخيص GPL) | **مؤجل** — لا تحسمه قبل تدقيق رسمي للرخص؛ نفّذ الآن سياسة 4.1 (تثبيت مصدر + hash + كاش) |
| نوع التحديث | Installer موقّع **عند مرحلة النشر المستقر**؛ مؤقتًا Portable + SHA-256 منشور |
| سجل Rust | احذفه فقط إن ثبت أنه بلا مستخدم؛ وإلا سجل منظّم ومحدود — لا كود ميت |

## ملخص الأولويات

| الأولوية | المرحلة | التأثير |
|----------|---------|---------|
| 🔴 عاجل | 0، 1 | خط أساس + صحة النتيجة |
| 🟠 عالٍ | 2، 3 | تجربة التشغيل وإدارة العمليات |
| 🟡 متوسط | 4، 5، 6 | قابلية التدقيق والتنظيف والجودة |
| 🟢 تحسين | 7 | الميزات |

</div>
