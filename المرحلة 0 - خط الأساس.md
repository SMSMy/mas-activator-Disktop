<div dir="rtl">

# المرحلة 0 — خط الأساس وتوثيق الحالة الحالية

> توثيق سلوك النسخة 2.1.0 قبل أي تعديل — نتائج تدقيق أولي (أغسطس 2026).
> ملاحظة: هذا توثيق يعمل عليه في النسخة الجانبية `mas-activator-rust-v2` — الأصل لم يُمس.

## بنية المشروع

```
client/            React 19 + TS + Tailwind + Framer Motion (واجهة عربية RTL)
server/            خادم Express — غير مستخدم في تطبيق Tauri (كود ميت محتمل)
shared/            ثوابت مشتركة
src-tauri/         Rust (Tauri 2.x) — الخلفية الفعلية
```

## ما نُفّذ فعلاً (تدقيق)

| البند | الحالة |
|---|---|
| رفع UAC | ✅ `src-tauri/build.rs` يضمّن manifest بـ `requireAdministrator` + Common Controls 6 + supportedOS Win10/11 |
| نظام سجل في Rust | ⚠️ `AppState.logs` + `get_logs` + `clear_logs` — **غير مستخدمة من الواجهة** (السجل محلي في React) وتنمو بلا حدود |
| فحص التحديثات | ⚠️ `check_update` عبر GitHub API — لا تمييز بين «لا تحديث» و«تعذر التحقق» (403/429 تُبتلع) |
| تحميل التحديث | ⚠️ يكتب في `USERPROFILE\Downloads` مباشرة — يتجاهل مجلد التنزيلات الفعلي، اسم الملف غير معقّم |
| اختبارات | ❌ لا توجد أي اختبارات (Rust أو TS) |
| CI | ❌ لا يوجد |

## الأخطاء المؤكدة (خط الأساس)

1. ~~ترميز `src-tauri/tauri.conf.json`~~ — **تم التحقق بالبايتات: الملف سليم UTF-8** والعنوان «أداة التفعيل الشاملة» صحيح؛ الـ mojibake كان خلل عرض في طرف المراجعة فقط. البند 1.1 ملغى.
2. **فحص حالة ويندوز يعتمد على slmgr الإنجليزي** (`lib.rs:263-286`):
   regex على `permanently activated` / `will expire` / `grace` — لا يتطابق
   على ويندوز غير إنجليزي (عربي) فيرجع نصًا خامًا.
3. **كشف نجاح/فشل التفعيل غير موثوق** (`execute_activation`):
   `$LASTEXITCODE` بعد استدعاء ScriptBlock لا يعكس كود خروج MAS،
   و`exit` داخل سكربت MAS قد يُغلق PowerShell قبل طباعة `ACTIVATION_OK|`
   → احتمال «نجاح زائف» («تم التنفيذ»).
4. **لا مهلات ولا إلغاء**: `irm` قد يعلق للأبد؛ إغلاق التطبيق يترك
   PowerShell يتيمًا.
5. **منطق زر «فحص الحالة»** (`Home.tsx:215`): يتأثر بحالة تحميل أي عملية
   أخرى (`key === "status" ? true`).
6. **إصدار مكرر يدويًا** (`Home.tsx:171`: «الإصدار 2.1») رغم وجوده في
   Cargo.toml / tauri.conf.json.
7. **بقايا أدوات تطوير**: `client/public/__manus__/debug-collector.js`،
   `vite-plugin-manus-runtime`، `.manus-logs/`، `.webdev/`.
8. **تبعيات غير مستخدمة** (من قالب shadcn): recharts, react-hook-form, zod,
   vaul, cmdk, streamdown, express, axios... إلخ.

## مصفوفة القبول (قائمة يدوية + أتمتة الرخيص)

| مجال الاختبار | سيناريوهات القبول | النتيجة بعد التنفيذ |
|---|---|---|
| حالة الترخيص | Windows عربي/إنجليزي، منتجات متعددة، مرخص/غير مرخص/سماح/تعذر | ☐ |
| الصلاحيات | تشغيل عادي (UAC)، رفض، كمسؤول | ☐ |
| الشبكة | متاحة / منقطعة / مهلة / استجابة غير صالحة | ☐ |
| إدارة العمليات | إلغاء، مهلة، إغلاق نافذة، ثم فحص مباشر | ☐ |
| الواجهة | لا تداخل مؤشرات، نتيجة مفهومة لكل خطأ | ☐ |
| البناء | `pnpm check` + بناء + `cargo test` + `cargo clippy` | ☐ |

## قرارات معتمدة من الخطة

- رفض UAC = إنهاء الإطلاق بلا رسالة داخلية (موثق؛ المشغّل غير المرتفع تطوير مستقبلي).
- انتهاء المهلة ≠ إنهاء العملية — يجب قتل شجرة العملية صراحة.
- hash-pinning = تحقق سلامة وليس بديلًا عن توقيع مستقل.

</div>
